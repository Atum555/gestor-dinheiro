[project]
name = "gestor-dinheiro"
version = "0.1.0"
description = "Gestor de dinheiro pessoal"
requires-python = ">=3.14"
dependencies = [
    "pandas>=2.2",
    "sqlalchemy>=2.0",
]

[tool.uv]
package = false
